# Automatically added by RStudio (ignores the RStudio project file).
^.*\.Rproj$

# Used for temporary files.
^\.Rproj\.user$

# Omit the Quarto file used to generate README.md.
^README\.Qmd$

# Omit folders created by building vignettes.
^doc$
^Meta$

# Omit log files created by knitting to PDF.
^inst/*.log

# Omit any software license files.
^LICENSE\.md$
^LICENSE
^LICENSE\.note$

# Exclude .Rdata files and placeholder file.
^data/*\.Rdata$
^data/*\.RData$
^data/*\.Rds$
^data/Placeholder.txt$

# Omit the folders for files that only need to be in the local repository, not
# in the installed package.
^scripts$

